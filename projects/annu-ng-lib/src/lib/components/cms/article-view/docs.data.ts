import { DocsData } from '../../docs/docs.interface';
import { ARTICLE } from '../article/docs.data';

export const ArticleViewComponent: DocsData = {
    projectionContent: '',
    inputPropsValues: {
        value: { ...ARTICLE }
    }
}
