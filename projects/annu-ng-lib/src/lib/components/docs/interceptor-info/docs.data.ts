import { DocsData } from '../../docs/docs.interface';
const interceptorInfo = {};

export const InterceptorInfoComponent: DocsData = {
    projectionContent: '',
    inputPropsValues: {
        interceptorInfo
    }
}
